#!/usr/bin/env zsh
p="$HOME/.vim/config/"
now=$(date)
/bin/cat $p"*" |\
grep -v fugitive |\
grep -v ^$ |\
egrep -v ^' '\{0,5}\"|\
sed 's/muttclone/darkblue/' > ~/Gits/mobileVimrc/.vimrc
cd ~/Gits/mobileVimrc/
git add --all
git commit -q -m "update on $now"
git push -q
